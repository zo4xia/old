# 落地项目开发说明（明细版）

## 2026-05-30 批注

| 字段 | 内容 |
| --- | --- |
| 批注日期 | 2026-05-30 |
| 原记录状态 | 部分有效 |
| 当前真相文件 | `当前项目全局认知图与批注说明.md` |
| 仍然有效 | 项目定位、A/B/C 职责、只录舞台区域、整版板书预览是临时态、字段与路由契约必须贯穿，这些口径仍然有效。 |
| 已被覆盖 | “tldraw 优先作为统一画布框架底座”这条实现判断已过期；当前更准确的施工口径是：前三步主链正确，第四步舞台承载层可以重构，主舞台已切到 drawboard 路线并在验证 Konva 内容层 proof。 |
| 为什么覆盖 | 项目本质进一步明确为高仿在线直播板书解题录屏 / 板书教学、自动化工作流 / 多 agent 协作产品。继续把主舞台绑死在重编辑器范式上，会阻碍后续程序化控制、板书演绎和录屏稳定性。 |
| 下一步 | 保留旧记录作为历史足迹；新的舞台承载、四区标签来源、低分辨率 PC 约束、统一配置中心、系统字体 vs 手写字体边界，以批注文档为当前真相。 |

# 落地项目开发说明（明细版）

## 1. 项目定位（不可漂移）

本项目是**高仿直播板书解题工作台**，不是通用白板。
核心目标是：
1. 讲解链路可交付（A/B/C 主链稳定）
2. 板书视觉可提前评审（第三步可视化预览）
3. 录制结果可用（只录舞台区域）

---

## 2. 全局硬边界（必须遵守）

### 2.1 一套实现路径
- 同一能力只保留一条主路径。
- 能靠成熟框架（tldraw/浏览器原生）就不自研第二套。
- 禁止同语义混用多种实现（例如同语义同时硬编码 px 和百分比逻辑漂移）。
- tldraw 优先作为统一画布框架底座，按标准项目转换路线推进，禁止回退到手搓三无不稳定运行时。

### 2.2 定义贯穿
- 定义必须端到端贯穿：输入、转换、输出、UI 消费一致。
- 任一环定义漂移会导致全链路失真。

### 2.3 模块化调用
- 每个模块必须有清晰 `input / output / boundary`。
- 禁止把请求、状态、转换、渲染堆在单组件里。

## 2.5 节点闭环文档规则（强制）
- 每完成一个节点，先反思自审，再更新升级说明书与开发文档。
- 更新必须包含：字段变化、调用变化、边界变化、前端行为变化、验收证据。
- 记录目标是可复现：出现灾难或交接时，仅凭文档可完整恢复。


## 3. ABC 职责口径（唯一真相）

### A（语音主时钟）
- 负责：语音时长/播放时钟/source 时间锚点。
- 不负责：C 的视觉位置/字号/颜色/排版。

### B（寿命窗口）
- 负责：C 的显示窗口（何时出现/消失/留场）。
- 不负责：C 内容与视觉样式。

### C（画布演员）
- 负责：内容视觉呈现与演绎（位置、大小、字体、drawSpeed、观感）。
- 不负责：改写 A 音频时钟或 B 寿命真相。

---

## 4. 第三步新增能力（本轮核心）

## 4.1 目标
在第三步（语音生产前/时）新增按钮：
- `生成板书排版预览`

该按钮产出：
- **可视化整版板书预览**（给甲方提前看整体观感）
- 不是文本摘要，不是正式 C 真相

### 4.2 预览结果定位
- 显示位置：侧边预览区域。
- 数据身份：临时预览态（可丢失，不并入正式 timeline 真相）。
- 用途：评估疏密、层次、分区、阅读顺序。
- 语义边界：`打开查看` 是查看/编辑；`重新生成` 才是用户不满意后的显式再生成动作。

### 4.3 画布四区标签映射（必须）
这四个区域标签是后续 C 站位和排版来源：
- 题目 -> 开场读题
- 分析 -> 分析题目
- 解答 -> 解题步骤 A1/B1/C1...
- 总结 -> 梳理总结

---

## 5. 字段与路由契约（本轮）

## 5.1 新增能力：布局预览请求
建议新增路由：`POST /api/agent/board-layout-preview`

### Request
```json
{
  "problemText": "string",
  "rows": [
    {
      "id": "string",
      "section": "string",
      "stepLabel": "string",
      "voiceText": "string",
      "boardSlice": "string"
    }
  ],
  "stageCanvas": {
    "width": 1920,
    "height": 1080,
    "background": "#ffffff",
    "boardFontName": "...",
    "boardFontFamily": "...",
    "boardFontSize": 72
  }
}
```

### Response
```json
{
  "status": "ok",
  "preview": {
    "version": "c-layout-preview-v1",
    "items": [
      {
        "id": "preview-item-1",
        "rowId": "row-1",
        "text": "...",
        "xPercent": 30,
        "yPercent": 45,
        "widthPercent": 24,
        "fontSize": 68,
        "groupKey": "analysis",
        "stackIndex": 1
      }
    ]
  }
}
```

### Error
```json
{
  "status": "failed",
  "error": {
    "code": "...",
    "message": "..."
  }
}
```

---

## 6. 模块职责拆分（避免堆砌）

### 6.1 VoiceWorkspace
- 负责按钮编排与触发。
- 不负责布局算法本体。

### 6.2 Service Client
- 负责调用 `/api/agent/board-layout-preview`。
- 不负责 UI 状态渲染。

### 6.3 Store（临时预览态）
- 负责保存 `cLayoutPreviewDraft`（临时）。
- 不写正式 timeline B/C。

### 6.4 Preview Mapper
- 负责 `preview.items -> tldraw shapes` 映射。
- 不负责业务流程控制。

### 6.5 BoardPreviewCard
- 负责侧边可视化展示（整版）。
- 不负责正式数据持久化。

---

## 7. 单位与样式规范

- 壳层布局：px/rem（统一 token）。
- 舞台对象：x/y/width 使用百分比（相对布局）。
- 禁止同语义双单位并行漂移。
- 预览和主舞台尽量统一到 tldraw 承载。

---

## 8. 金手指（画笔）边界

- 模式：`off / pen / eraser` 必须互斥。
- 指针：off 透传；pen/eraser 拦截。
- 数据：只在 overlay 层，不污染 ABC base。
- 录制：可见层参与录制，但不反写 base 真相。

---

## 9. 录制边界（明确录什么/不录什么）

### 必录
1. 画布底板内容
2. C 板书可见结果
3. 金手指覆盖层
4. 讲解音频（A轨）

### 禁录
1. 左右侧栏
2. 顶部工具栏
3. 时间轴与播放控件
4. 弹窗/抽屉/浮动按钮
5. 鼠标指针与非舞台 UI

### 实现原则
- 优先浏览器原生能力。
- 不为帧率极限做重型自研录制系统。

---

## 10. 用户视角验收流程（必须跑通）

1. 上传题目并确认题文。
2. 打开 Agent 进入查看/编辑，用户按需点击“重新生成”得到 rows。
3. 进入第三步，点击 `生成板书排版预览`。
4. 侧边出现整版可视化预览。
5. 用户评估整体观感，不满意则回 Agent 调整。
6. 满意后再点击 `生成讲解音频`。
7. 后续按既有 A/B/C 主链播放与录制。

验收通过标准：
- 预览可见且反映整体板书效果。
- 预览不会污染正式 A/B/C timeline 真相。
- 音频生产链路无回归。
- 录制边界符合“只舞台、不壳层”。

---

## 12. 节点更新（2026-05-29）

### 节点A：四区标签硬约束落地
- 预览网关输出强制归一化为：题目 / 分析 / 解答 / 总结。
- fallback 排版按四区分组，不再纯顺序散排。
- 主舞台标签补齐四区。

### 节点B：tldraw能力恢复 + 保护边界并行
- 主舞台恢复原生 eraser 能力。
- 同时保留受管形状缺失修复，防止底层 ABC 元素静默损坏。

### 节点C：PC工作台布局收口
- 固定三列骨架收口到 420 / min620 / 280。
- 最小宽度调整为 1160，并加强主列可用性约束。


---

## 11. 本地保存/缓存与框架承载边界（补充）

### 11.1 本地保存与缓存策略（三层）
1. localStorage 轻快照（project/pendingProject/candidate/config）
2. Dexie IndexedDB 快照（current + saved）
3. 用户授权目录归档导出（project/oral-script/board/timeline/tts/audio）

要求：
- 这些策略是工程稳定性的基线，不能在重构中丢失。
- 播放态需避免高频持久化写入造成卡顿。

### 11.2 阿里云语音数学转义边界（必须保留）
- 统一入口：`prepareAliyunMathSpeechText()`
- 保留：分隔符识别、分数/根号/幂次口播映射、常见 LaTeX 损坏修复
- 禁止组件层自行拼接临时转义

### 11.3 数学公式/手写字体/默认字体边界
- 公式显示由 FormulaText(KaTeX)负责显示，不改业务真相
- 手写与公式分流由 mathBoardText 路由统一判断
- 字符支持边界必须以代码白名单和结构数学判定为准，变更需同步文档与检查脚本

**### 11.4 tldraw 承载边界 **  

(错误批注: tldraw太复杂了发现，我们正在重新改成简单的对agent友好的 Konva ） 
-* 以 tldraw 作为统一画布框架底座*
*- 业务真相由 TeachingProject/store 承载*
*- `layoutPreviewDraft` 仅临时态，不入正式 timeline 真相*
*- 禁止回退手搓三无运行时*
